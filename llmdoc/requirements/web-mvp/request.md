# web-mvp 原始需求

状态：已记录用户需求，等待 PM 在正式开工时编写明细 PRD。

## 用户明确要求

1. 产品为「万物说明书」：输入具体物品及型号、说明书、多视图，通过 Tripo API 生成 3D 模型并形成可交互说明书。
2. 产品调整为前后端分离的网站：前端 React，后端 Rust，最终能打包成一个二进制发布。
3. 技术方案和实施路径足够明确，可指导能力较弱的 agent 完成编码。
4. 项目内定义 Claude Code 四个角色：PM 读需求并写明细 PRD；UI 根据 PRD 完成交互设计并补入 PRD；RD 按 PRD 编码；QA 验收，有问题退回 RD 修复后复验。
5. 协作中无法体现在 code 中的信息，提炼重点到 `llmdoc/`，四角色都必须遵守，供子 agent 查阅。

## 本轮已经交付的范围

技术方案、数据与接口设计、实施任务卡、验收路径、四角色配置及启动入口。尚未启动真实生成和网站编码，不能据此声称已有运行产品。

## PM 开工依据

从 `../../README.md` 按顺序阅读当前方案。旧 macOS 文档只提供产品背景，新方案中的 React/Rust/GLB/单二进制约束优先；不要把旧 Swift 源码恢复设成网站开发前置条件。

方案默认首版单管理员自托管；持久数据在 data-dir；前端静态资源内嵌 Rust；说明书页准备在浏览器，服务端接受完整快照后继续后台任务。必要校准在浏览器完成，几何动画不作为首版自动准确承诺。PM 应把这些默认设计显式写入 PRD，并标明待样本验证的指标。
